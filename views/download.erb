<%= erb :menu, locals: { pageid: 'home' } %>

<% content_for :sub_heading do %>
  Download <%= role %> data
<% end %>

<table class="primary">
  <thead>
    <tr>
      <th>File Type</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= role %></td>
      <td><a href="<%= url %>/fieldforce/file/<%= role %>">Download</a></td>
    </tr>
  </tbody>
</table>
